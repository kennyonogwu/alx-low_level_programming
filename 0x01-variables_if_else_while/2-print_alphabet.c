#include <stdio.h>
/**
 * main the the entry point of the program
 *
 * Return: Always set to 0 (Succes)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
{
	putchar(c);
}
	putchar('\n');
return (0);
}
